import React from 'react'
import {IMAGE_BASE_URL} from 'config';
import 'components/elements/Actor/Actor.css'

const Actor = (props) => (
    <div>
        <h1>Actor</h1>
    </div>
)

export default Actor
