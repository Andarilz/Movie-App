import React from 'react'
import 'components/elements/Spinner/Spinner.css'

const Spinner = (props) => {

    return (
        <div className='loader'>

        </div>
    )
}

export default Spinner
